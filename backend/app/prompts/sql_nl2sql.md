Generate one PostgreSQL SELECT against the employees table only.
Return SQL only, no markdown.
Never use INSERT/UPDATE/DELETE/DROP/ALTER.
Always include LIMIT <= 200.
Respect that salary may be restricted by role (caller validates columns).
